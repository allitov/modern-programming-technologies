# Правила разработки проекта

## 1. Стек технологий

Проект использует следующий стек:

- Java 25
- Gradle
- Lombok
- JUnit 5
- AssertJ

### Зависимости

- Новые зависимости **добавлять не нужно**.
- Все необходимые зависимости для выполнения заданий уже подключены в проекте.
- Перед добавлением любой зависимости необходимо убедиться, что требуемая функциональность не предоставляется уже существующими зависимостями.

---

## 2. Code Style

### 2.1. Форматирование полей класса

Между полями класса необходимо оставлять пустую строку.

Правильно:

```java
public class User {

    private String name;

    private String email;

    private int age;
}
```

Неправильно:

```java
public class User {

    private String name;
    private String email;
    private int age;
}
```

---

### 2.2. Lombok

Для стандартных методов класса необходимо использовать аннотации Lombok.

К стандартным методам относятся:

- getters;
- setters;
- constructors;
- `equals`;
- `hashCode`;
- `toString`.

Если стандартную реализацию метода можно заменить соответствующей Lombok-аннотацией, необходимо использовать Lombok.

Например:

```java
@Getter
@Setter
@NoArgsConstructor
@AllArgsConstructor
@EqualsAndHashCode
@ToString
public class User {

    private String name;

    private String email;
}
```

Аннотации Lombok следует размещать **на уровне класса**, если это возможно.

Не следует вручную реализовывать стандартные getters, setters, constructors, `equals`, `hashCode` и `toString`, если для них подходит Lombok.

Если параметров в конструкторе больше 3, то следует использовать аннотацию `@Builder`.

---

## 3. Javadoc

### 3.1. Язык

Javadoc должен быть написан **на русском языке**.

Пример:

```java
/**
 * Вычисляет итоговую стоимость заказа с учетом скидки.
 *
 * @param total исходная стоимость заказа.
 * @param discount размер скидки.
 * @return итоговая стоимость заказа.
 */
public double calculateTotal(double total, double discount) {
    return total - discount;
}
```

---

### 3.2. Javadoc для методов интерфейса

Javadoc должен располагаться **над публичными методами интерфейса**.

Пример:

```java
public interface DiscountService {

    /**
     * Вычисляет стоимость заказа с учетом скидки.
     *
     * @param total стоимость заказа.
     * @return стоимость заказа после применения скидки.
     */
    double applyDiscount(double total);
}
```

---

### 3.3. Javadoc для методов, объявленных только в реализации

Если метод не объявлен в интерфейсе и существует только в классе-реализации, Javadoc необходимо писать непосредственно над методом в реализации.

Пример:

```java
public class DiscountServiceImpl {

    /**
     * Проверяет, является ли заказ достаточно большим для получения скидки.
     *
     * @param total стоимость заказа.
     * @return {@code true}, если заказ подходит для скидки.
     */
    private boolean isEligibleForDiscount(double total) {
        return total >= 5000;
    }
}
```

---

## 4. Тестирование

Для написания тестов необходимо использовать:

- JUnit 5;
- AssertJ.

Необходимо использовать уже подключенные в проект зависимости.

При наличии нескольких тестовых сценариев предпочтительно использовать параметризованные тесты JUnit 5, если это делает тесты более компактными и понятными.

Для проверки результатов использовать AssertJ.

Пример:

```java
assertThat(result)
        .isEqualTo(expected);
```

---

# 5. Общий алгоритм выполнения задания

При выполнении каждого задания ИИ-агент должен:

1. Изучить существующую структуру проекта.
2. Найти связанные с заданием классы, интерфейсы и тесты.
3. Изучить существующий код перед внесением изменений.
4. Реализовать требуемую функциональность.
5. Соблюдать правила Code Style из данного файла.
6. Не добавлять новые зависимости.
7. Добавить или обновить Javadoc на русском языке.
8. Добавить необходимые тесты на JUnit 5.
9. Использовать AssertJ для проверок в тестах.
10. Запустить тесты.
11. Исправить найденные ошибки.
12. Повторно запустить тесты после исправлений.
13. Выполнить итоговую проверку по чеклисту ниже.

---

# 6. Чеклист после выполнения задания

Перед завершением задания необходимо проверить каждый пункт.

## Code Style

- [ ] Между полями класса присутствуют пустые строки.
- [ ] Стандартные getters реализованы через Lombok.
- [ ] Стандартные setters реализованы через Lombok.
- [ ] Стандартные constructors реализованы через Lombok.
- [ ] `equals` реализован через Lombok, если требуется.
- [ ] `hashCode` реализован через Lombok, если требуется.
- [ ] `toString` реализован через Lombok, если требуется.
- [ ] Lombok-аннотации размещены на уровне класса, где это возможно.
- [ ] Нет лишней ручной реализации методов, которые может заменить Lombok.

## Javadoc

- [ ] Javadoc написан на русском языке.
- [ ] Javadoc присутствует над публичными методами интерфейсов.
- [ ] Если метод объявлен только в реализации, Javadoc находится над методом реализации.
- [ ] Javadoc содержит корректные `@param` для параметров.
- [ ] Javadoc содержит `@return` для возвращаемого значения, если это необходимо.
- [ ] Javadoc соответствует фактическому поведению метода.

## Тесты

- [ ] Для тестов используется JUnit 5.
- [ ] Для проверок используется AssertJ.
- [ ] Проверены основные сценарии работы метода.
- [ ] Проверены граничные значения, если они применимы.
- [ ] Проверены ошибочные сценарии, если они предусмотрены заданием.
- [ ] Параметризованные тесты используются там, где они упрощают набор тестов.
- [ ] Все тесты проходят успешно.

## Проверка результата

- [ ] Проект успешно компилируется.
- [ ] Все тесты проходят.
- [ ] Новых зависимостей нет.
- [ ] Изменения соответствуют исходному заданию.
- [ ] Не изменены несвязанные с заданием части проекта.
- [ ] Код соответствует правилам данного файла.

---

# 7. Финальная проверка

Перед завершением работы ИИ-агент должен убедиться, что:

> **Задание выполнено, проект собирается, все тесты проходят, новые зависимости не добавлены, Javadoc написан на русском языке, а код соответствует правилам Code Style.**
